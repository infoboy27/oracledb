version: '3.8'

services:
  ords:
    build: .
    container_name: ords-test
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - JAVA_OPTS=-Xmx512m
    networks:
      - ords_net

networks:
  ords_net:
    driver: bridge

